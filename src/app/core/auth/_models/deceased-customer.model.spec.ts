import { DeceasedCustomer } from './deceased-customer.model';

describe('DeceasedCustomer', () => {
  it('should create an instance', () => {
    expect(new DeceasedCustomer()).toBeTruthy();
  });
});
